export type RouteName = 'home' | 'basket' | 'admin';

export const ROUTE_NAME_HOME = "home" as const;
export const ROUTE_NAME_BASKET = "basket" as const;
export const ROUTE_NAME_ADMIN = "admin" as const;
